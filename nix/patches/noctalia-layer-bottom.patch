--- a/src/wayland/layer_surface.cpp
+++ b/src/wayland/layer_surface.cpp
@@ -42,6 +42,9 @@ LayerShellLayer layerShellLayerFromConfig(std::string_view layer) {
   if (layer == "overlay") {
     return LayerShellLayer::Overlay;
   }
+  if (layer == "bottom") {
+    return LayerShellLayer::Bottom;
+  }
   if (layer == "top") {
     return LayerShellLayer::Top;
   }
--- a/src/config/schema/config_schema.cpp
+++ b/src/config/schema/config_schema.cpp
@@ -2157,6 +2157,6 @@
             if (auto v = tbl["layer"].value<std::string>()) {
-              if (*v == "top" || *v == "overlay") {
+              if (*v == "top" || *v == "overlay" || *v == "bottom") {
                 out.layer = *v;
               } else {
                 diag.warn(joinPath(parentPath, "layer"), "expected top or overlay, got \"" + *v + "\"");
               }
--- a/src/shell/panel/panel_manager.cpp
+++ b/src/shell/panel/panel_manager.cpp
@@ -840,8 +840,10 @@
       && outputWidth > 0
       && outputHeight > 0;
   const LayerShellLayer floatingPanelLayer = resolveFloatingPanelLayer(m_config, m_activePanelId, *m_activePanel);
+  // If the bar is on the bottom layer, attached panels must still open above windows.
+  const LayerShellLayer barLayer = layerShellLayerFromConfig(barConfig.layer);
   const LayerShellLayer panelLayer =
-      useAttachedPlacement ? layerShellLayerFromConfig(barConfig.layer) : floatingPanelLayer;
+      (useAttachedPlacement && barLayer != LayerShellLayer::Bottom) ? barLayer : floatingPanelLayer;
   m_panelLayer = panelLayer;
